{
  "name": "jira-researcher",
  "description": "Conducts iterative Jira research to answer questions using JQL queries, generates concise answers with documented JSON sources. Input: working_folder (optional, auto-generates research-results/[yyyy-mm-dd]-[topic] if not provided), question (research query)"
}
